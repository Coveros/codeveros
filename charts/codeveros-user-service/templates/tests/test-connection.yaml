apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "codeveros-user-service.fullname" . }}-test-connection"
  labels:
{{ include "codeveros-user-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "codeveros-user-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
